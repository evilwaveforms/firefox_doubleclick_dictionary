{
  "name": "firefox-doubleclick-dictionary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node test/dictionary.test.mjs",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
